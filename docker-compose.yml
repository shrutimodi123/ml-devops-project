version: "3.8"

services:
  app:
    build: .
    container_name: flask_app_container
    ports:
      - "5000:5000"
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: redis_container
    ports:
      - "6379:6379"